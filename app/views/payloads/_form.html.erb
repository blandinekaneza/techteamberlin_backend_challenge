<%= form_with(model: payload, local: true) do |form| %>
  <% if payload.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(payload.errors.count, "error") %> prohibited this payload from being saved:</h2>

      <ul>
      <% payload.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :payload_id %>
    <%= form.number_field :payload_id %>
  </div>

  <div class="field">
    <%= form.label :reused %>
    <%= form.check_box :reused %>
  </div>

  <div class="field">
    <%= form.label :manufacturer %>
    <%= form.text_area :manufacturer %>
  </div>

  <div class="field">
    <%= form.label :payload_type %>
    <%= form.text_area :payload_type %>
  </div>

  <div class="field">
    <%= form.label :payload_mass_kg %>
    <%= form.number_field :payload_mass_kg %>
  </div>

  <div class="field">
    <%= form.label :payload_mass_lbs %>
    <%= form.number_field :payload_mass_lbs %>
  </div>

  <div class="field">
    <%= form.label :orbit %>
    <%= form.text_area :orbit %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
